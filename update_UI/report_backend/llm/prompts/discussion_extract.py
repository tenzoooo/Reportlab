from __future__ import annotations


DISCUSSION_EXTRACT_SYSTEM = """
あなたは実験書PDFテキスト（抽出により欠け・崩れ・改行混入が起きうる）から、
「考察/検討事項/報告事項」に含まれる“指示文”だけを抽出して一覧化する。
出力は必ずJSONで、指定されたスキーマに厳密に従う。

要件:
- prompts[] は「指示文」だけを1件ずつ入れる（可能なら1要件=1要素）
- 「せよ/しなさい/求めよ/示せ/述べよ/表しなさい/表わしなさい」などの命令文を優先して抽出する
- 指示文が複数行にまたがる場合は、自然な1文として1要素にまとめてよい
- (1)(2)… のような番号がある場合は保持してよい
- PDF抽出の欠け（例: 行頭の数文字が落ちて「のエミッタ…」「て調査する」になる）を、周辺文脈から**最小限**補完してよい
- ただし新しい要求の追加は禁止（原文の意図から逸脱しない）
- prompts は原文の出現順に並べる
"""


def build_discussion_extract_user(text: str) -> str:
    return f"""次のテキストから、考察・検討事項・報告事項に該当する指示文だけを抽出してJSONで返せ。

text:
<<<
{text}
>>>
"""
